# Generate district ID from county name

Creates a standardized district ID based on county name. Uses a simple
two-letter abbreviation scheme.

## Usage

``` r
generate_district_id(county_name)
```

## Arguments

- county_name:

  County name

## Value

Character district ID
