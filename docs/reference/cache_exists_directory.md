# Check if cached directory data exists

Check if cached directory data exists

## Usage

``` r
cache_exists_directory(cache_type, max_age = 30)
```

## Arguments

- cache_type:

  Type of cache

- max_age:

  Maximum age in days (default 30). Set to Inf to ignore age.

## Value

Logical indicating if valid cache exists
